rules_version = '2';
service cloud.firestore {
  match /databases/{database}/documents {
    // Mason Market routes all database access through Next.js server APIs
    // using Firebase Admin SDK. Admin SDK bypasses these rules.
    // Keep direct client access blocked by default.
    match /{document=**} {
      allow read, write: if false;
    }
  }
}
